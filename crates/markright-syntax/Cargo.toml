[package]
name = "markright-syntax"
version = "0.2.0"
edition = "2024"
description = "Syntax highlighting wrapper for MarkRight"
license = "AGPL-3.0-only"

[dependencies]
syntect = { version = "5", default-features = false, features = ["default-syntaxes", "default-themes", "html", "regex-fancy"] }
